# ТЗ (MVP) v2: Telegram-бот для отзывов/вопросов WB/Ozon

## 0) Цель

Сервис в Telegram для владельца/агентства: подключить несколько кабинетов WB/Ozon, собирать **отзывы и вопросы**, генерировать ответы через ИИ, управлять KB (базой знаний), публиковать ответы вручную или автоматически по настройкам.

## 1) Сущности и доступы

### 1.1 Пользователи и проекты

* **Owner** — владелец (создаёт проекты, управляет балансом).
* **Проект** — единица бренда/клиента.
* **Кабинет МП** — подключение конкретного кабинета WB/Ozon. В проекте стандартно 3–5 кабинетов.
* **Сотрудник** — Telegram-пользователь с доступом к проекту.

### 1.2 Роли

* **Админ**: всё, включая сотрудников, подключение кабинетов, настройки, KB, баланс.
* **Менеджер**: **только KB + ответы** (обработка отзывов/вопросов, добавление/удаление правил KB).
* **Наблюдатель**: всё как у менеджера, но **без редактирования** (read-only).

### 1.3 Уровень прав

* Доступы выдаются **на уровне проекта** (не на уровне кабинета).

## 2) Авторизация

* MVP: только Telegram (по Telegram user_id).

## 3) Поддерживаемые МП

* MVP: **Wildberries + Ozon**.

## 4) Товары и идентификация (обязательное требование)

* В системе обязателен **внутренний SKU**.
* Сразу нужна **кросс-идентификация** SKU между WB и Ozon (один SKU → несколько marketplace item ids).
* KB привязывается к **SKU** или **всему проекту**.

## 5) Получение событий (отзывы/вопросы)

* Приоритет: webhook, если доступно.
* Если webhook недоступен: polling каждые 1 минуту.
* В БД храним:

  * нормализованные поля события (id, тип, текст, оценка, даты, SKU, статус)
  * **raw payload**
  * ссылки на медиа (фото/видео), без хранения файлов

## 6) Токены/ключи API кабинетов

* Храним в БД (шифрование).
* После ввода показываем только маску.
* Ввод через бот: мастер-диалог + проверка подключения.

## 7) Автогенерация и автоотправка

Настройки проекта:

* **5/4⭐ отзывы**: автогенерация (да/нет), автоотправка (да/нет)
* **1–3⭐ или негатив**: автогенерация (да/нет), автоотправка (да/нет)
* **Вопросы**: автогенерация (да/нет), автоотправка (да/нет)
* По умолчанию: **approve обязателен**, но автоотправка должна быть доступна в MVP.
* Автоотправка блокируется, если LLM adapter вернул `conflict = true` или `confidence` ниже порога (low confidence).

## 8) Логика ИИ

* ИИ предлагает черновик и метку уверенности **в %**.
* «Не хватает информации»: не найдено KB или найден конфликт.
* «Конфликт KB»: под один запрос подходят **2+ ответа/правила** и они **противоречат**.
* Жёстких глобальных запретов нет — всё через KB проекта.

### 8.1 Интерфейс LLM adapter (абстрактный провайдер)

**Цель:** единая обвязка для разных LLM-провайдеров с одинаковым контрактом результата.

**Контракт генерации (ответ LLM adapter):**

* `text` — сгенерированный текст ответа.
* `confidence` — уверенность (0–100).
* `kb_rule_ids` — список ID правил KB, использованных в генерации.
* `conflict` — признак конфликта KB (true/false).

**Правила эскалации:**

* Если `conflict = true` **или** `confidence` ниже порога (low confidence), то:
  * автоотправка **запрещена**;
  * требуется **ручной approve** менеджером/админом.

**Примечание:** порог low confidence задаётся настройкой проекта (дефолт: 70).

## 9) Эскалация

* Эскалация = отправка на обычный approve менеджеру.
* После эскалации можно вернуть в работу.

## 10) База знаний (KB)

* Минимальная карточка правила:

  * уровень: **проект** или **SKU**
  * тип: **факт**
  * текст
  * дата добавления
* Управление KB: список правил + удаление.
* Версионность не нужна.

## 11) Биллинг и подписка

* Единицы: **внутренние токены**. 1 генерация = 1 токен.
* «100 бесплатно»: 100 токенов/месяц.
* Баланс общий на owner.
* Баланс списывается **только у owner**.
* Если нет подписки на канал: **бот ничего не делает**.
* Если баланс = 0: стоп **генераций** и стоп **парсинга** новых отзывов/вопросов.

## 12) Очередь задач

* В очередь уходит:

  * polling/webhook обработка
  * генерация ИИ
  * автоотправка
  * обработка ссылок на медиа

## 13) Хранение и ретеншн

* Логи/аудит: 90 дней.
* Отзывы/вопросы (нормализованные данные) храним дольше 90 дней.
* Медиа не храним, только ссылки.

## 14) Логи и аудит

Фиксируем:

* кто отправил ответ
* какой prompt/model/version
* какие KB правила использовались
* raw payload
* статусы: new / drafted / approved / sent / escalated / error

---

# Telegram-Навигация (структура MVP)

## A) Глобальная модель навигации

* Бот поддерживает **несколько проектов** у одного пользователя.
* Всегда есть текущий контекст: **Текущий проект**.
* Главная навигация через меню-кнопки (ReplyKeyboard) + внутри карточек Inline кнопки.

## B) Экран 0: /start

1. Проверка подписки на канал

   * Если нет подписки: экран «Подпишись, чтобы пользоваться» + кнопка «Проверить подписку»
2. Если подписка есть:

   * Если нет проектов: кнопка «➕ Создать проект»
   * Если есть проекты: «Выбрать проект»

## C) Экран 1: Выбор проекта

* Список проектов (кнопки по 10/страница)
* Кнопка «➕ Новый проект» (только админ)
* Кнопка «⚙️ Настройки» (только админ)

## D) Экран 2: Главная проекта (Dashboard)

Плитки/кнопки:

* «📝 Лента» (отзывы+вопросы)
* «📚 KB»
* «🏪 Кабинеты» (только админ)
* «⚙️ Настройки проекта» (только админ)
* «💳 Баланс» (только админ)
* «📊 Статистика» (всем, read-only)

Показываем кратко:

* Баланс токенов (если админ)
* Новые/без ответа/эскалации (счётчики)

## E) Экран 3: Лента (список)

* По умолчанию показываем 10 элементов
* Для каждого элемента: тип (отзыв/вопрос), оценка/тональность, SKU, дата, статус

### E1) Фильтры ленты

Inline кнопки под заголовком:

* Новые
* Без ответа
* Answered
* Эскалация
* SKU (выбор из списка SKU или ввод SKU — TBD)
* Тональность (позитив/нейтрал/негатив)

Навигация:

* «⬅️ Назад»
* «◀️ Пред» / «След ▶️» (пагинация)

## F) Экран 4: Карточка отзыва/вопроса

Поля:

* Тип: отзыв/вопрос
* Текст
* SKU (и кросс-id WB/Ozon если есть)
* Оценка (если отзыв)
* Тональность (если определена)
* Дата
* Медиа: ссылки/превью
* Статус
* Предложенный ответ (если сгенерен)
* Уверенность %
* KB источники (список правил)

Кнопки:

* ✅ Отправить
* ✏️ Править
* ♻️ Перегенерировать
* ➕ Добавить правило
* 🚫 Эскалация
* ⬅️ Назад в ленту

## G) Экран 5: Правка ответа

* Показываем текущий текст ответа
* Пользователь отправляет новое сообщение текстом
* Inline: «✅ Сохранить» / «↩️ Отмена»

## H) Экран 6: Добавить правило KB (мастер)

Шаги:

1. Уровень: Проект / SKU
2. Если SKU: выбор SKU (или ввод — TBD)
3. Текст правила (сообщением)
4. Подтверждение: «✅ Добавить»

## I) Экран 7: KB (список)

* Фильтр: Проектные / SKU-правила
* По 10 элементов
* Каждое правило: дата + короткий текст
* Действия:

  * «🗑 Удалить» (админ/менеджер)
  * «⬅️ Назад»

## J) Экран 8: Кабинеты (админ)

* Список кабинетов (WB/Ozon)
* «➕ Добавить кабинет»
* «🔄 Проверить подключение»
* «🗑 Удалить кабинет»

## K) Экран 9: Онбординг кабинета (админ)

Шаги:

1. Выбор МП (WB/Ozon)
2. Ввод токена
3. Тест подключения
4. Подтверждение + маска токена

## L) Экран 10: Настройки проекта (админ)

* Тон бренда (краткое описание)
* Настройки автогенерации/автоотправки:

  * 5/4⭐
  * 1–3⭐/негатив
  * вопросы
* Поведение при конфликте KB: эскалация/блокировать автоотправку (TBD)

## M) Экран 11: Баланс (админ)

* Текущий баланс
* «➕ Пополнить» (Telegram Payments)
* История списаний (по генерациям) — TBD

---

# Финализированные решения (зафиксировано)

## Идемпотентность и события

* Уникальность события определяется по **ID отзыва / вопроса**, предоставляемому маркетплейсом (WB/Ozon).
* При повторном приходе события с тем же ID — **новая запись не создаётся**.
* Если отзыв/вопрос был **изменён покупателем**, это считается **новым событием** и попадает в ленту как отдельная сущность.

## SLA

* SLA **5 минут**: время от **публикации отзыва/вопроса покупателем на платформе** до **сгенерированного черновика ответа** (при включённой автогенерации).

## Поведение при нулевом балансе

* При балансе = 0:

  * генерация ИИ остановлена
  * парсинг новых отзывов/вопросов остановлен
* После пополнения пользователь выбирает:

  * **A)** обработать все неотвеченные отзывы/вопросы, накопившиеся за период
  * **B)** получать и обрабатывать **только новые** события с момента пополнения

## SKU и кросс-идентификация

* При получении событий бот **пытается автоматически сопоставить артикулы** между WB и Ozon (по одинаковым seller SKU).
* Если остаются нераспределённые позиции, бот предлагает пользователю варианты:

  1. сопоставить артикулы через **шаблон (таблица)**
  2. создать **новые SKU** в системе
* Артикулы могут **регулярно обновляться**.
* Пользователь должен иметь возможность **обновлять и пересопоставлять SKU через шаблон** в любой момент.

## Автоотправка и конфликты KB

* Если обнаружен конфликт KB или низкая уверенность:

  * автоотправка **запрещена**
  * событие отправляется на **ручной approve менеджеру**

## Подписка на канал

* Без подписки бот показывает **только экран**:

  * «Подпишись на канал»
  * кнопка «Проверить подписку»
* Любые другие действия недоступны.

---

# Финализировано (дополнение)

## Шаблон сопоставления SKU

* Формат шаблона: **XLSX**.
* Шаблон используется для:

  * первичного сопоставления SKU между WB/Ozon
  * массового пересопоставления существующих SKU

## Массовое пересопоставление SKU — логика

* Отзывы/вопросы **всегда привязаны к SKU внутри маркетплейса**.
* При изменении сопоставления SKU → товар:

  * все существующие отзывы/вопросы **остаются привязанными к SKU**
  * автоматически **отображаются у нового товара**, к которому привязан SKU
* Исторические события **не дублируются и не пересоздаются**.

## Лимиты на шаблоны

* В MVP ограничения на:

  * размер файла
  * количество строк
  * частоту загрузки
    **не вводятся**.
* Возможность добавления лимитов отложена на post-MVP.

---

# TBD (минимальный хвост)

1. UI уведомления при массовом пересопоставлении SKU (текст подтверждения действия).
2. Нужен ли preview изменений перед применением шаблона (optional, можно post-MVP).

# Принятые уточнения (добавлено)

* Формат SKU-шаблона: **XLSX**.
* Лимиты на размер и частоту загрузки шаблонов: **в MVP без ограничений** (но на бэкенде стоит поставить базовую защиту: лимит размера файла и антифлуд).
* Пересопоставление и история: отзывы/вопросы привязаны к **seller SKU внутри МП**. Если seller SKU перепривязали к другому внутреннему SKU, то **вся история по seller SKU отображается у нового внутреннего SKU**.

# XLSX-шаблон для SKU (добавлено)

## Назначение

Используется для:

* первичного сопоставления seller SKU между WB/Ozon
* массового обновления и пересопоставления SKU

## Формат файла

* Формат: **XLSX**
* Один файл = один проект
* Лист по умолчанию: `SKU_MAPPING`

## Колонки (MVP)

1. `marketplace` — enum: `WB` / `OZON`
2. `seller_sku` — артикул продавца в МП (string, обязательное)
3. `marketplace_item_id` — ID товара в МП (nmId / offerId и т.п.)
4. `internal_sku` — внутренний SKU системы (string)
5. `product_name` — название товара (опционально, для удобства)

## Правила обработки

* `(marketplace + seller_sku)` — уникальная связка
* Если `internal_sku`:

  * существует → выполняется перепривязка
  * не существует → создаётся новый внутренний SKU
* Все отзывы/вопросы **всегда привязаны к seller_sku МП**
* При перепривязке seller_sku к другому internal_sku:

  * вся история отзывов/вопросов начинает отображаться у нового товара

## Валидация

* Обязательные поля: `marketplace`, `seller_sku`
* Невалидные строки:

  * пропускаются
  * логируются
  * отображаются пользователю отчётом после загрузки

## Ограничения (MVP)

* Продуктовых ограничений нет
* Технические предохранители:

  * лимит размера файла (TBD, например 20 МБ)
  * антифлуд загрузки (TBD)

---

# Технические предохранители (рекомендация, зафиксировано)

Даже при отсутствии продуктовых лимитов, на уровне backend должны быть:

* ограничение размера загружаемого файла
* защита от частых повторных загрузок
* таймаут обработки XLSX

---

# Проверка полноты ТЗ для Codex

## Закрыто и достаточно для реализации

* роли и права
* авторизация
* сущности (проект / кабинет / SKU / отзывы / KB)
* идемпотентность событий
* логика автогенерации и автоотправки
* логика конфликтов KB
* биллинг и токены
* остановка парсинга
* Telegram-навигация (экраны, кнопки, фильтры)
* аудит и логи
* SLA

## Места, где Codex может потребовать решения (если не уточнять — выбрать дефолт)

### 1) Очередь задач (важно)

Нужно выбрать реализацию:

* Redis + RQ
* Celery + Redis
* FastAPI BackgroundTasks (не рекомендуется)

**Если не указано явно:** Celery + Redis.

### 2) Модель ИИ

Нужно указать:

* провайдер (OpenAI / другое)
* модель (TBD)

**Если не указано:** абстрактный LLM-провайдер через адаптер.

### 3) Хранение файлов XLSX

* локально на VPS
* временно в памяти

**Если не указано:** временное хранение + удаление после обработки.

### 4) Формат хранения уверенности ИИ

* число 0–100

**Если не указано:** integer 0–100.

### 5) Язык интерфейса

* RU (MVP)

---

# Итог

ТЗ **достаточно полное для старта разработки Codex**.
Все критические продуктовые и технические решения зафиксированы.
Оставшиеся пункты — безопасные дефолты, которые можно принять без участия продакта.
